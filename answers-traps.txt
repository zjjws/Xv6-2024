1.RISC-V 的函数参数在 a0–a7 里； 可以找到对应代码为：24:	4635                	li	a2,13，所以 13 在 a2。
2.没调用 f 和 g，这两个函数在main中的调用被编译器直接内联优化掉了，main中代码被转化为等价的 printf("%d %d\n",12,13);
3.0x6bc
4.main 中 jal 6bc <printf> 这条指令在地址 0x30，所以返回地址 ra 会被设置成下一条指令地址 0x34（也就是 li a0,0 那条）
5.结果为 He110 World。
57616 是十六进制 0xe110，%x 打印 e110
i=0x00646c72 在小端内存里字节序是 72 6c 64 00，也就是字符串 "rld"

如果在大端下，这个时候 i 需要转化为 0x726c6400。

6.“y=” 后会打印一个不确定/不可预测的垃圾值，因为格式串需要两个 %d 但是只传入了一个参数，printf 会继续从“下一个参数位置”（寄存器/栈里当作参数的地方）取值，但是不能确定这个值是什么。